//! Reserved for dragontools-maintenance; not built or installed in this milestone.
//! Future systemd oneshot+timer, no listener, remote-command API, or control plane.
pub fn check() error{NotImplemented}!void {
    // TODO: TLS, OS/security metadata, pinned component update checks, atomic metrics.
    return error.NotImplemented;
}
